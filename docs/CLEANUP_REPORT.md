# Cleanup Report

**Date:** 2026-06-10
**Scope:** Repository hygiene before the portfolio commit. Conservative by design —
nothing in source, tests, docs, config, or public assets was removed.

## Summary

The repository was already well-structured with a comprehensive `.gitignore`, so
cleanup was light. The main risks (build output, `node_modules`, secrets) were
already handled by ignore rules; this pass verified that and tidied a few stray
local artifacts.

## Files removed (local working tree only — all were git-ignored)

| Item | Why it was unnecessary | Rollback |
| --- | --- | --- |
| `next-9003.err.log`, `next-9003.out.log` | Dev-server stdout/stderr logs from a manual `next dev -p 9003` run | Regenerated automatically if you run a dev server; nothing to restore |
| `.next-debug-dev.err.log`, `.next-debug-dev.out.log` | Stale debug dev-server logs | Same as above |
| `.next-debug/`, `.next-debug-dev/` | Old Next.js debug build caches (from May) | Regenerated by `next build`/`next dev` |
| `.debug-run/` | Stale local debug-run artifacts | Regenerated by tooling if needed |
| `.next-build/` | Temporary build dir created during this pass to dodge a Windows `.next` lock; no longer needed | Recreated only if you set `NEXT_DIST_DIR` |

All of the above match existing `.gitignore` patterns, so removing them does not
change what Git tracks — it only reduces local clutter.

## Files kept intentionally

| Item | Reason |
| --- | --- |
| All `src/`, `tests/`, `tools/` | Source, tests, and the screenshot tool |
| All `docs/*.md` | Architecture, education, and portfolio docs — none are duplicates (verified) |
| `docs/blueprint.md` | Original design spec — useful historical context |
| `docs/ASTAR_IMPLEMENTATION_REVIEW.md` | Decision record of the pre-Euclidean A\* state; intentionally historical |
| `docs/screenshots/*.png` (1.7 MB) | Verified portfolio screenshots referenced by the README |
| `README.md`, `LICENSE`, `package.json`, `netlify.toml`, `next.config.ts`, `tailwind.config.ts`, `tsconfig.json` | Project + build configuration |
| `src/lib/translations.ts`, `src/lib/algorithm-explanations.ts` | Translation + explanation content |
| `out/` (local, ignored) | Current static build — handy for re-running screenshots; not committed |
| `.modified`, `.netlify/`, `node_modules/`, `.next/`, `tsconfig.tsbuildinfo` | Ignored; left in place |

## Files NOT touched (out of scope)

- The parent folder `C:\Users\Blerim\Desktop\FEBSc\CIT\` contains academic
  `*.docx` / `*.pptx` documents. These are **outside the git repository**, are
  not tracked, and are the author's personal documents — left entirely untouched.

## `.gitignore` changes

Added `/.next-build/` to the existing "Local debug / dev-server artifacts"
section so the temporary build directory is never accidentally tracked. The
existing rules already cover `node_modules/`, `/.next/`, `/out/`, `/coverage`,
`/build`, `.env`/`.env*.local`, `*.tsbuildinfo`, `*.err.log`, `*.out.log`,
`.netlify/`, and IDE files.

## Pre-commit safety verification

- `git check-ignore` confirms `node_modules`, `.next`, `out`, `.netlify`,
  `tsconfig.tsbuildinfo`, and `.env` are all ignored.
- `git status` confirms **none** of those heavy/ignored directories appear in the
  pending commit.
- No `.env` files exist in the repo.
- Source scanned for secrets (Google API-key patterns, `firebaseConfig`,
  `apiKey`/`PRIVATE KEY` literals): **none found**.

## Dependency hygiene

- Added `sucrase` to `devDependencies` — the test scripts (`node -r
  sucrase/register/ts …`) depend on it, but it was previously only present
  transitively. This prevents a fresh `npm install` from breaking the tests.
- Added `playwright` to `devDependencies` for the screenshot tool.
